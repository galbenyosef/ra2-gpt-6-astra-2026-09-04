import {chromium} from '@playwright/test';
import assert from 'node:assert/strict';
import {writeFile,mkdir} from 'node:fs/promises';
const origin=process.env.TEST_ORIGIN||'http://127.0.0.1:4175';
await mkdir('.cache/viewer-tests',{recursive:true});
const b=await chromium.launch({channel:'chrome',headless:true});try{
const p=await b.newPage({viewport:{width:1440,height:950},locale:'zh-CN'});const errors=[],requests=[];p.on('pageerror',e=>{errors.push(e.message);console.log(e.message);});p.on('request',r=>requests.push(r.url()));
await p.goto(origin);await p.waitForFunction(()=>window.__viewer);assert.equal(await p.locator('.canvas-link').getAttribute('href'),'/canvas/');await p.click('.canvas-link');await p.waitForFunction(()=>window.__hd?.ready,{},{timeout:60000});assert.equal(new URL(p.url()).origin,origin);assert.equal(new URL(p.url()).pathname,'/canvas/');await p.waitForTimeout(200);assert.equal(await p.evaluate(()=>__hd.renderer.ctx.constructor.name),'CanvasRenderingContext2D');assert.equal(await p.evaluate(()=>__hd.actors.length),8);assert(await p.evaluate(()=>Object.values(__hd.hd.sprites).every(s=>s.src.startsWith('/canvas/sprites/'))));const testedOriginal=await p.locator('#toggle').isEnabled();if(testedOriginal){await p.click('#toggle');assert.match(await p.locator('#mode').innerText(),/原版/);await p.click('#toggle');}await p.screenshot({path:'.cache/viewer-tests/integrated-canvas.png'});
await p.locator('header a').click();await p.waitForFunction(()=>window.__viewer);assert.equal(new URL(p.url()).pathname,'/');await p.evaluate(()=>__viewer.add('/samples/low','30K'));await p.evaluate(()=>__viewer.ready());assert.equal(await p.evaluate(()=>__viewer.stats()[0].triangles),30000);assert.deepEqual(errors,[]);await writeFile('.cache/viewer-tests/integrated.json',JSON.stringify({origin,samePort:true,canvasContext:true,actors:8,originalSwitch:testedOriginal,backToGLB:true,errors,canvasAssetRequests:requests.filter(u=>u.includes('/canvas/sprites/')).length},null,2));console.log('PASS integrated GLB / Canvas 2D');}finally{await b.close();}
